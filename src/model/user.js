export default class User {
    constructor(username, nickname, email, password, role)
    {
        this.username = username;
        this.nickname = nickname;
        this.email = email;
        this.password = password;
        this.role = role;
    }
}